require 'spec_helper'

describe ComicsController do

	before do 
		@series = Series.new(series_attributes)
	end

	context "when not signed in as an authorized user" do 

		it "cannot accesst new" 
	
		it "cannot access create"

		it "cannot access delete"

	end

end