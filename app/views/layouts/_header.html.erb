<h1 id="header"><%= link_to "Comics", series_index_path %></h1>

<% if current_user %>
	<ul id="sign-out">
		<li>Hi, <%= link_to current_user.name, current_user %>!</li>
		<li><%= link_to "Sign Out", session_path, method: :delete %></li>
	</ul>
<% end %>